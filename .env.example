OPENAI_API_KEY=sk-...
# Image model: gpt-image-2 (recommended), gpt-image-1, gpt-image-1.5
OPENAI_IMAGE_MODEL=gpt-image-2

# Provider: openai (default — production). hairgen = archived spike only; not used.
IMAGE_PROVIDER=openai

# Rate limiting (POST /api/generate — per client IP)
RATE_LIMIT_PER_HOUR=10
# DAILY_GENERATION_CAP=100
# RATE_LIMIT_DISABLED=true

# Legal deploy gate (TL.4 — production only)
# When true on Vercel production, blocks /api/generate until counsel approves legal pages.
# LEGAL_PAGES_REQUIRED=true
# COUNSEL_LEGAL_APPROVED=true
